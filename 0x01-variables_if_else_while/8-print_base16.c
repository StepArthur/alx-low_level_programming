#include <stdio.h>

/**
 * main - A program to print base 16 numbers
 * Return: 0 (success)
 */
int main(void)
{
	int h;
	char l;

	for (h = '0'; h <= 10; h++)
	putchar(h);

	for (l = 'a'; l < 'g'; l++)
	putchar(l);
	putchar('\n');
	return (0);






}
